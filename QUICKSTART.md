# 快速上手

**Helmsman 控制台已部署在服务器，无需本地安装任何东西。**

---

## 第一步：开 SSH 隧道

```bash
ssh -L 8080:127.0.0.1:8080 -p 20023 root@45.78.224.156
```

> 保持这个终端窗口开着，隧道断了控制台就断了。

---

## 第二步：打开控制台

浏览器访问 **http://localhost:8080**

---

## 第三步：发起任务

首页填写：

| 字段 | 说明 |
|------|------|
| 域名 | 目标网站，如 `vndb.org` |
| 需求 | 自然语言描述，如"采集 galgame 游戏资料，每条含标题/简介/标签" |
| 发起人 | 你的名字（荣誉制，便于区分谁的任务） |
| 模式 | `auto`（推荐）= 先侦察再采集；`explore` = 只侦察；`harvest` = 只采集 |

点 **发起** 后跳转到任务仪表盘，可以实时看进度。

---

## 使用中

- **实时浏览器**：点右上角"连接"可以看到 agent 正在操作的浏览器画面
- **需要你介入**：页面会弹横幅 + 浏览器通知（记得允许通知权限）
  - `Gate 检查点` — 侦察完了，确认是否继续采集
  - `需要你回答` — agent 或 intake 有问题要问你
  - `需要你介入` — 登录/验证码/2FA，在右侧实时浏览器里操作完点"完成"
- **历史任务**：首页列出所有跑过的任务，点进去可以翻记录

---

## 产出在哪

任务结束后数据在服务器的：

```
/root/full-self-crawl-agent-beta/artifacts/{域名}/runs/{run_id}/samples/
```

可以用 `scp` 拉到本地：

```bash
scp -r -p 20023 root@45.78.224.156:/root/full-self-crawl-agent-beta/artifacts/vndb.org/runs/20260610-xxx/samples/ ./
```

---

## 注意事项

- **别两个人同时跑同一个域名**——同域名共享登录状态，会互相干扰
- 不同域名可以同时跑（默认最多 6 个并发）
- 首页看到别人的任务不要随便点"停止"
- 控制台无密码，只有能 SSH 进服务器的人才能访问
